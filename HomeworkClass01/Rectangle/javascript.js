/*Write a JavaScript program that takes length and width of a rectangle
and finds its perimeter. Print the result in the console.*/

// Perimeter of Rectangle = (L + W) × 2

let length = 5;
let width = 8;
let perimeter = (length + width) * 2;
console.log(perimeter);