/*Write a JavaScript program that takes the base and height of a triangle
and calculates its area. Print the result in the console.*/

// Area of triangle = 1/2 × base × height

let base = 5;
let height = 8;
let area = 1/2 * base * height;
console.log(area);